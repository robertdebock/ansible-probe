---
_galaxy_plaform:
  amazonlinux: Amazon
  alpine: Alpine
  archlinux: ArchLinux
  centos: EL
  debian: Debian
  fedora: Fedora
  opensuse: OpenSUSE
  oraclelinux: EL
  redhat: EL
  rockylinux: EL
  ubuntu: Ubuntu

galaxy_platform: "{{ _galaxy_plaform[image.name] }}"
